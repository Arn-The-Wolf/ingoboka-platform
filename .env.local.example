# Local development — talk to API on localhost
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1

# Production (Vercel) — same-origin proxy avoids mixed-content + CORS
# NEXT_PUBLIC_API_BASE_URL=/api/v1
# API_PROXY_TARGET=http://185.181.10.165:8085

# Server-only: marketing catalog proxy (never expose password to browser)
# CATALOG_SERVICE_EMAIL=citizen.demo@ingoboka.rw
# CATALOG_SERVICE_PASSWORD=Ingoboka@2026

# Optional
# NEXT_PUBLIC_APP_URL=http://localhost:3000
# NEXT_PUBLIC_ENABLE_PWA=false
