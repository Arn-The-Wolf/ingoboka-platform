/** Default axios timeout for JSON API calls (ms). */
export const DEFAULT_API_TIMEOUT_MS = 90_000;

/** Multipart uploads (claim evidence, profile pictures) — MinIO proxy can be slow. */
export const UPLOAD_API_TIMEOUT_MS = 180_000;
